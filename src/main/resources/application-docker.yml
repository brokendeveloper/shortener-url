spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/shortener-url-db
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080

app:
  shortener:
    base-url: "http://localhost:8080/"