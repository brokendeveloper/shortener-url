server:
  port: 8080

spring:
  application:
    name: shortener-url

  datasource:
    url: jdbc:postgresql://localhost:5433/shortener_url_db
    username: YOUR_DATABASE_USERNAME
    password: YOUR_DATABASE_PASSWORD
    driver-class-name: org.postgresql.Driver
